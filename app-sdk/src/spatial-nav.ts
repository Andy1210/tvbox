// Single import site for spatial-navigation init, so apps don't each depend on
// the norigin package path directly.
export { init as initSpatialNavigation } from "@noriginmedia/norigin-spatial-navigation";
